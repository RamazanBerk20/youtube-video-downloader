# yt-dlp pinned to a version that supports the `remote_components` option
# (yt-dlp's anti-bot EJS challenge solver) and `js_runtimes`. 2025-09 added
# js_runtimes; 2025-12 added remote_components.
yt-dlp>=2025.12.0
customtkinter>=5.2.2
# Arabic shaping: presentation-form glyphs + RTL bidi algorithm.
# Without these, Tk 8.6 renders Arabic letters in isolated form.
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
